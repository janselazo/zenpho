/** Primary label for `/booking` CTAs (matches site-wide default button copy). */
export const BOOKING_PRIMARY_BUTTON_LABEL = "Book a Free Build Call";

/** Navbar / compact breakpoints where the full primary label does not fit cleanly. */
export const BOOKING_NAV_COMPACT_BUTTON_LABEL = "Book a Call";
